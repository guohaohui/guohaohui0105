# 一、选择题

```c
    1. D
    2. B
    3. A
    4. D
    5. B
    6. D
    7. C
    8. D
    9. A
    10. B
```

## 二、填空题

```c

    1.if((c >='A' && c <= 'Z') || (c >= 'a' && c >= 'z'))

    2. for(i = 1; i <= 101; i+=2>)

    3. if(a[i] < 0)

    4. 11   13 1859

    5.  int len = strlen(src);
    *d++ = *s--;

```

## 三、简答题

```c

1. static全局变量与普通全局变量的区别是static全局变量只初始化一次，防止在其他文件单元被引用。
static局部变量与普通局部变量的区别是static局部变量未初始化时值为0，存放在bss段初始化后放在数据段并且延长变量生命周期到整个程序结束，普通局部变量未初始化值随机存放在栈中初始化后也存放在栈中，生命周期随着函数的调用结束而结束。

2. 应用层、传输层、网络层、链路层
3.
4.
5.
```

## 四、编程题

```c


```
